[package]
name = "llama-cpp-ffi"
version = "0.1.0"
edition = "2021"
build = "build.rs"

[dependencies]
libc = "0.2"

[build-dependencies]
bindgen = "0.71"
cmake = "0.1"

[features]
default = ["mtmd"]
mtmd = []
metal = []
cuda = []
